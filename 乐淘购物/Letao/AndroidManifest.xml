<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jclt.activity"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/jclt"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name=".FirstActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".NewProMarketActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".SecondActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".CommonActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".BoyShoesActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".GirlShoesActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".MoreShoesActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".MoreInforActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".MyLetaoInforActivity" />
        <!-- 底部菜单模块<更多>Activity -->
        <activity
            android:label="@string/app_name"
            android:name=".more.LetaoServceActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".more.LetaoAboutActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".more.LetaoHistoryActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".more.LetaoHotLineActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".more.LetaoSuggestActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:label="@string/app_name"
            android:name=".more.LetaoMicroBlogActivity" />
        <!-- 底部菜单模块<我的乐淘>Activity -->
        <activity
            android:label="@string/app_name"
            android:name=".myletao.LetaoAddressActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".myletao.LetaoCollectActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".myletao.LetaoFavorableActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".myletao.LetaoIndentActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".myletao.LetaoPasswordActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".myletao.ExitUserActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".myletao.RegisterActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".myletao.ReturnPasswordActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".myletao.subclass.LetaoNewAddressActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".myletao.subclass.LetaoAddFavorableActivity" />
        <!-- 底部菜单模块<我的购物车>Activity -->
        <activity
            android:label="@string/app_name"
            android:name=".shopping.ShoppingCarActivity" />
        <!-- 底部菜单模块<我的分类> -->
        <activity
            android:label="@string/app_name"
            android:name=".type.TypeLetaoActivity" />
        <activity
            android:label="@string/app_name"
            android:name=".type.CommondityInforLetaoActivity" >
        </activity>
    </application>

    <uses-permission android:name="android.permission.BROADCAST_STICKY" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CALL_PRIVILEGED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
    <uses-permission android:name="android.permission.RESTART_PACKAGES" />

</manifest>